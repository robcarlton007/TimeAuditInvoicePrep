# ── Build output ──────────────────────────────────────────────────────────────
dist/
build/
__pycache__/
*.pyc
*.pyo
*.spec.bak

# ── Local data — never commit these ───────────────────────────────────────────
# Cache and session files contain employee names, project keys, and API tokens
# email_config.json contains live Gmail app password — keep excluded
*.json

# Audit report output
audit_*.html
audit_*.txt
raw_dump_*.csv

# ── Credentials / secrets ─────────────────────────────────────────────────────
.env
*.env
secrets.json
luca_config.json

# ── Large binaries ────────────────────────────────────────────────────────────
# The logo PNG is 1.4 MB — commit a lower-res version if needed, or use Git LFS
# luca_logo.png   ← uncomment this line if you want to exclude it
Gemini_Generated_Image_*.png

# ── Windows ───────────────────────────────────────────────────────────────────
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# ── Editor artifacts ──────────────────────────────────────────────────────────
.vscode/
.idea/
*.bak
*.bak2
*.bak3
fix_fonts.py
rebuild_cache.py
build_output.txt
design_mockups/
